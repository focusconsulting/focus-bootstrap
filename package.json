{
    "name": "tools",
    "version": "0.1.0",
    "description": "Tools and scripts for the project",
    "engines": {
        "node": ">=18",
        "npm": ">=9"
    },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "prepare": "husky",
        "start": "docker-compose up --detach",
        "stop": "docker-compose down"
    },
    "devDependencies": {
        "husky": "^9.0.11",
        "lint-staged": "^15.2.2"
    },
    "dependencies": {
        "date-fns": "^3.6.0",
        "lodash": "^4.17.21"
    }
}
